# Local development — uses DATABASE_URL directly via psycopg2
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Production (Cloud Functions) — uses Cloud SQL Python Connector
# Set these instead of DATABASE_URL:
# INSTANCE_CONNECTION_NAME=project:region:instance
# DB_USER=postgres
# DB_PASS=your-password
# DB_NAME=requests_test
